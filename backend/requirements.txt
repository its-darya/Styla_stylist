# Backend (FastAPI). Install the ML stack from the root requirements.txt first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#   pip install -r backend/requirements.txt
fastapi
uvicorn[standard]
python-multipart
pydantic>=2
python-dotenv>=1.0

# Auth
PyJWT>=2.8
bcrypt>=4.1

# Database (PostgreSQL + pgvector)
psycopg[binary]>=3.1
psycopg_pool>=3.2
pgvector>=0.2.5

# Vision
rembg[cpu]
onnxruntime
Pillow
scipy
transformers>=4.40

# Virtual try-on (HF Spaces client)
gradio_client
requests
